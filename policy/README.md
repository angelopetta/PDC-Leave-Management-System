# Policy

The authoritative leave policy lives in the KI HR handbook. This directory
contains the machine-encoded version used by the application.

- `rules.ts` — encoded policy (see PRD Appendix A).
